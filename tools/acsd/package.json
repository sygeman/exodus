{
  "name": "@acsd/cli",
  "version": "0.2.0",
  "type": "module",
  "description": "ACSD Reconcile Tool with SQLite",
  "scripts": {
    "helper": "bun cli/bin/acsd-helper.ts",
    "test": "bun test tests/"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
